{
  "name": "pinata-server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "pinata": "^2.4.4",
    "react": "^19.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
